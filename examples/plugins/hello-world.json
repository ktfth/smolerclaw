{
  "name": "hello_world",
  "description": "Say hello — the simplest possible smolerclaw plugin.",
  "input_schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name to greet"
      }
    },
    "required": ["name"]
  },
  "command": "echo Hello, {{input.name}}! Welcome to smolerclaw."
}
